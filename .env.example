# Keep the product free while validating demand.
FREE_BETA=true
PORT=8787

# Enable only after a hosted checkout and webhook-backed entitlement service exist.
BILLING_CHECKOUT_BASE_URL=
BILLING_WEBHOOK_SECRET=

# MuseWave-owned GPU engine. Leave empty until a trained checkpoint is deployed.
MUSEWAVE_ENGINE_URL=
MUSEWAVE_ENGINE_TOKEN=
